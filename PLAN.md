# Site plan — what to add next

## The journey (one unbroken scroll)
Hero (bg video, dims to dark) → cue zooms into waveform → voice → letters →
words → sentence cleans itself (colour-coded edits) → card dissolves into
Gmail → Slack speed moment → Notion writes itself → iMessage sends →
"Wherever the cursor goes…" → Messages face cross-fades into Notion →
speak-to-edit → recedes into the app wall → closing statement → bento
features → live demo.

## Assets needed (blocking polish)
- [ ] `public/hero-voice.mp4` — bright ~10s loop of voice prompting
  (screen recording of the widget over a light app). Bright animated
  gradient stands in until this lands.
- [ ] Real product screenshots for the app cards (replace mimicked UIs).
- [ ] OG image / social card, favicon set.

## Sections to add
- [ ] **Download / install strip** — `brew install …` one-liner with copy
  button + direct .dmg link, pinned right under the hero CTAs and repeated
  after the finale.
- [ ] **Pricing** (even if "free forever" — say it loudly, CRED-style card).
- [ ] **Comparison strip** — Verbatim vs macOS dictation vs superwhisper
  (latency, privacy, cleanup quality).
- [ ] **Testimonials / wall of love** — tweets marquee.
- [ ] **FAQ** — privacy, model, languages, offline, shortcuts.
- [ ] **Changelog / roadmap teaser** — pulls from GitHub releases.
- [ ] **Footer upgrade** — sitemap, socials, "built by SaaS Labs" credit.

## Polish backlog
- [ ] Sound-reactive hero capsule (mic permission optional easter egg).
- [ ] Keyboard-scrubbing (arrow keys step between beats).
- [ ] Mobile pass — scenes currently tuned for desktop; collapse deck to a
  swipe carousel under 640px.
- [ ] Lighthouse pass: poster frame for the video, `prefers-reduced-data`.
- [ ] Analytics events per scene (how far do people scroll?).
